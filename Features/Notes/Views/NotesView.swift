import SwiftUI
import SwiftData

struct NotesView: View {
    var body: some View {
        NavigationStack {
                Text("Anotações")
                    .font(.title2)
                    .foregroundColor(Color.colorGreen)
                    .bold()
                    .padding()
            
        }
    }
}

