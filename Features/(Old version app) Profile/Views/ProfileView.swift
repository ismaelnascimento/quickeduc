import SwiftUI
import SwiftData

struct ProfileView: View {
    var body: some View {
        NavigationStack {
                Section {
                    Text("Perfil")
                        .font(.title2)
                        .foregroundColor(Color.colorGreen)
                        .bold()
                        .padding()
                }
        }
    }
}
